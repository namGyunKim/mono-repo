{
  "name": "admin-api",
  "projectType": "application",
  "sourceRoot": "apps/admin-api/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew :apps:admin-api:build",
        "cwd": "."
      }
    },
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew :apps:admin-api:bootRun",
        "cwd": "."
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "./gradlew :apps:admin-api:test",
        "cwd": "."
      }
    }
  }
}
